---
labels: ['my app', 'app', 'app type']
description: 'A MyApp app component.'
---

A basic Bit app component.

## Run the app

```
# use the app
bit use my-app

# run the app
bit run my-app
```

## Compose the app

Compose the app to create your own instance of this app.

```ts
export default MyApp.from({});
```
